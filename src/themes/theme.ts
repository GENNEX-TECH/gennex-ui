import { renderTheme } from '@/utilities';

export const lightTheme = renderTheme({ mode: 'light' });
export const darkTheme = renderTheme({ mode: 'dark' });

export const blueLightTheme = renderTheme({ mode: 'light', preset: 'blue' });
export const blueDarkTheme = renderTheme({ mode: 'dark', preset: 'blue' });

export const grassLightTheme = renderTheme({ mode: 'light', preset: 'grass' });
export const grassDarkTheme = renderTheme({ mode: 'dark', preset: 'grass' });

export const tomatoLightTheme = renderTheme({ mode: 'light', preset: 'tomato' });
export const tomatoDarkTheme = renderTheme({ mode: 'dark', preset: 'tomato' });

export const indigoLightTheme = renderTheme({ mode: 'light', preset: 'indigo' });
export const indigoDarkTheme = renderTheme({ mode: 'dark', preset: 'indigo' });

export const violetLightTheme = renderTheme({ mode: 'light', preset: 'violet' });
export const violetDarkTheme = renderTheme({ mode: 'dark', preset: 'violet' });

export const orangeLightTheme = renderTheme({ mode: 'light', preset: 'orange' });
export const orangeDarkTheme = renderTheme({ mode: 'dark', preset: 'orange' });

export const cyanLightTheme = renderTheme({ mode: 'light', preset: 'cyan' });
export const cyanDarkTheme = renderTheme({ mode: 'dark', preset: 'cyan' });

export const amberLightTheme = renderTheme({ mode: 'light', preset: 'amber' });
export const amberDarkTheme = renderTheme({ mode: 'dark', preset: 'amber' });

export const slateLightTheme = renderTheme({ mode: 'light', preset: 'slate' });
export const slateDarkTheme = renderTheme({ mode: 'dark', preset: 'slate' });

export const mauveLightTheme = renderTheme({ mode: 'light', preset: 'mauve' });
export const mauveDarkTheme = renderTheme({ mode: 'dark', preset: 'mauve' });
